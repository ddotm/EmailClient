<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>net8.0;net10.0</TargetFrameworks>
		<LangVersion>latest</LangVersion>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<OutputType>Exe</OutputType>
	</PropertyGroup>

	<ItemGroup>
		<ProjectReference Include="..\src\IdempotentCookie.Email.Mailgun\IdempotentCookie.Email.Mailgun.csproj" />
		<ProjectReference Include="..\src\IdempotentCookie.Email.SendGrid\IdempotentCookie.Email.SendGrid.csproj" />
		<ProjectReference Include="..\src\IdempotentCookie.Email.Smtp\IdempotentCookie.Email.Smtp.csproj" />
	</ItemGroup>

</Project>